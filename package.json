{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node -e \"const fs = require('fs'); ['package-lock.json', 'yarn.lock'].forEach(f => fs.existsSync(f) && fs.unlinkSync(f)); if (!/pnpm/.test(process.env.npm_config_user_agent || '')) { console.error('Use pnpm instead'); process.exit(1); }\"",
    "dev": "npx pnpm --filter @workspace/api-server --filter @workspace/meet --parallel dev",
    "start": "npx pnpm --filter @workspace/api-server run start",
    "server": "npx pnpm --filter @workspace/api-server run start:local",
    "client": "npx pnpm --filter @workspace/meet run dev",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "vercel-build": "pnpm run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "packageManager": "pnpm@11.8.0",
  "devDependencies": {
    "prettier": "^3.8.3",
    "socket.io-client": "^4.8.3",
    "typescript": "~5.9.3"
  }
}
